// big O

// big O is a way to compare the runtime between algorithm.
// there are 
// O(1) O(logn) O(n) O(nlogn) O(n^2) O(n^3) O(2^n) and more,
// but we will stick with these.
// Orders of big O:
// 2222 + n = O(n) since n is O(n), 2222 is O(1), and O(n) > O(1)
// n + n = O(n) since O(2n) = O(n) constant gets ignore when multiplying
// O(n) * O(log(n)) = O(nlog(n)) 
// O(1) * O(n^2) = O(n^2)
// O(n) * O(n) = O(n^2)
// muliplying is straight forward